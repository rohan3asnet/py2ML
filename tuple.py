# list is for storing items that can change throughout the life of program
# values that cannot change are refered as immutable
# immutable list is called Tuple
# tuple used parantheses() instead of square brackets[]
dimensions=(10, 20, 30)
for dimension in dimensions:
    print(dimension)